(function(exorts)
{
    expor
    exports.Parser = {
        parse: function(str) {
            console.log(str);
        }
    };

}(typeof exports === 'undefined' ? this['Seqdiag'] = {} : exports );
